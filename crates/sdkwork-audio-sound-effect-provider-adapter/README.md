# SDKWork Audio Sound-Effect Provider Adapter

L4 adapter that converts the stable sound-effect SPI into the existing audio engine port. It does
not use raw HTTP or claim a generated SDK route that does not exist.
